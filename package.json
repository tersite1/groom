{
  "name": "kaiilbo",
  "version": "1.0.0",
  "private": true,
  "description": "카이일보 - AI 연구·안전·정책·생활 AI 전문 정적 뉴스 사이트",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "npm run validate",
    "serve": "node build.mjs && node --run serve:only",
    "serve:only": "npx --yes serve dist -l 4173",
    "dev": "node build.mjs && python3 -m http.server 4173 --directory dist"
  }
}
